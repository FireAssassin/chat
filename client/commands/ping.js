/* const readline = require("readline");

readline
    .createInterface({
        input: process.stdin,
        output: process.stdout,
        terminal: false,
    })
    .on("line", (input) => {
        if (input.match(/^\/ping/)) {
            console.log(ping());
        }
    });

function ping() {
    
} */